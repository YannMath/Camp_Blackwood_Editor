package Objects.GameObjects;

public class Player extends Entity {
    public Player(String sprite, String board, int x, int y, String name, int maxHealth) {
        super(sprite, board, x, y, name, maxHealth);
    }
}
